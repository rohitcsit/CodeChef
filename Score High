#include <bits/stdc++.h>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t--){
	    int n;
	    cin>>n;
	    int na,nb,nc,nd;
	    cin>>na>>nb>>nc>>nd;
	    int maxi = max(na,nb);
	    int maxii = max(nc,nd);
	    cout<<max(maxi,maxii)<<endl;
	}

}
